apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "triggered-rollout-restart-operator.fullname" . }}-test-connection"
  labels:
    {{- include "triggered-rollout-restart-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "triggered-rollout-restart-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
